// Leads-Tabelle (wird in Step 3 implementiert)
export default function LeadsSeite() {
  return (
    <div className="flex items-center justify-center min-h-[60vh]">
      <p className="text-slate-400">Leads-Tabelle wird in Step 3 implementiert …</p>
    </div>
  );
}
